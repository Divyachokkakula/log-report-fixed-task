FROM python:3.12-slim@sha256:<approved_harbor_base_image_digest>

RUN pip install --no-cache-dir \
    pytest==8.4.1 \
    pytest-json-ctrf==0.3.5

WORKDIR /app

COPY access.log /app/access.log


File removed from the build context:

solution_hint.py (it leaks the reference solution and must not be copied into the agent image).
